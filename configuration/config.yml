imports:
    -
        resource: di.yml
    -
        resource: config.env.yml

parameters:
    debug: true
    application.name: 'HAL 9000 Agent'
    application.version: '2.0-alpha'

### imported from hal-core
    doctrine.mappings:
        - '%annotations_suck%'
    doctrine.devmode: true
    doctrine.connection:
        driver:     pdo_mysql
        host:       localhost
        user:       root
        password:
        dbname:     hal_orm2