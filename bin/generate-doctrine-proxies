#!/usr/bin/env php
<?php

namespace QL\Hal\Bin;

use QL\Hal\Agent\Application\Di;
use QL\Hal\Core\Utility\DoctrineProxyGenerator;

$root = __DIR__ . '/..';
require_once $root . '/configuration/bootstrap.php';
$di = Di::getDi($root, 'QL\Hal\Agent\CachedContainer');

$generator = $di->get('doctrine.proxy.generator');

$success = $generator();
echo $generator->output();

if (!$success) {
    exit(1);
}
