#!/usr/bin/env sh

set -e

curdir=$( cd "$( dirname "$0" )" && pwd )

app="$1"
env_name="$2"
git_ref="$3"

build_id=$($curdir/hal build:create $app $env_name $git_ref --porcelain)

$curdir/hal build:build $build_id
